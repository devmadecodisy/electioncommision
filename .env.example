# Example environment variables for election-app
# Copy to `.env` (or set in your deployment) and update `VITE_API_URL` to point at your backend.
# Vite exposes variables that start with `VITE_` to the client application.

VITE_API_URL=https://api.example.com
